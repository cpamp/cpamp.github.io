{
  "dependencies": {
    "html-minifier-terser": "^7.2.0"
  },
  "scripts": {
    "build": "html-minifier-terser --input-dir src --output-dir . --file-ext html --collapse-whitespace --remove-comments --minify-css true --minify-js true && node scripts/check-size.cjs"
  }
}
